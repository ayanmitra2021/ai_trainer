# Production environment — loaded automatically by Vite during `npm run build`.
# VITE_API_BASE_URL tells the frontend where the FastAPI backend lives.
# Do NOT add trailing slash.
VITE_API_BASE_URL=https://ai-trainer-pavl.onrender.com
