"""Rollup Reporter Agent — DEPRECATED (Phase 9.1).

This agent has been removed from the active product as of Phase 9.1.
The archived implementation lives in ``backend/app/agents/_deprecated/rollup_reporter.py``.

Do not import from this module. It exists only so that old references fail loudly
rather than with a confusing ModuleNotFoundError from the archived copy.
"""

raise ImportError(
    "rollup_reporter is deprecated and was removed in Phase 9.1. "
    "See backend/app/agents/_deprecated/rollup_reporter.py for the archived code."
)
